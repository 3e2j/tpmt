//! A BMG's contents, in the editable form a project keeps them as.
//!
//! Deprecated: `tpmt-pipeline` no longer produces this on unpack. Scope for
//! now is raw bytes plus a UI; this stays as a possible on-disk translation
//! layer to revisit later, not wired into the pipeline.

pub mod json;
