[package]
name = "tpmt"
description = "Modding toolkit for The Legend of Zelda: Twilight Princess."
version.workspace = true
edition.workspace = true
repository.workspace = true

[dependencies]
# Whole operations only. Anything reaching for a format crate belongs in the
# pipeline, not here.
tpmt-pipeline.workspace = true

thiserror.workspace = true
clap.workspace = true

[lints]
workspace = true
