# Assets

This folder holds `.iso` game files, which are *intentionally* gitignored. This project does not distribute any game assets.

Several tests depend on these ISOs, so copy or symlink your own into this directory to ensure a complete test run.
It's recommended to test against multiple game versions for compatability.
