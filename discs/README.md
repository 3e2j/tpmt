# Discs

This folder holds `.iso` or `.ciso` game disc files, which are **intentionally** gitignored. This project does not distribute any game assets.

Several tests depend on these ISOs. You must copy or symlink your own into this directory; without them, a full test run is not possible.
It's recommended to test against multiple game versions for compatibility.
